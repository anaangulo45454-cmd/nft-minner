# VRAM MINER — Vercel Ready

A high-fidelity, original React/Vite recreation inspired by the supplied VRAM MINER source and public site structure.

## Run
npm install
npm run dev

## Build
npm run build

Deploy the repository directly to Vercel. Framework preset: Vite.
